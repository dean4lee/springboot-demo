server:
  port: 8080
  servlet:
    context-path: /

application:
  #版本号
  version: 1.0.0

dubbo:
  application:
    #应用名称，每个dubbo应用的名称都是唯一的
    name: dubbo-consumer
  registry:
    #注册中心
    address: zookeeper://172.16.77.131:2181
